# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: navigation.spec.js >> Navegação CineWeave >> navega entre as 4 abas
- Location: e2e\navigation.spec.js:23:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.screenplay-layout-container')
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('.screenplay-layout-container')

```

```yaml
- banner:
  - text: CineWeave
  - button "Roteiro"
  - button "Enciclopédia"
  - button "Mapa Mental"
  - button "Ideias"
  - text: Online
  - button "Guia"
  - button "Modo Escuro"
  - button "test"
  - button "▼"
- heading "Projetos" [level=2]
- button "Fechar"
- textbox "Filtrar por tags..."
- button [disabled]
- button "Sincronizar Tudo na Nuvem"
- heading "Novo Filme" [level=3]
- text: "Título do Roteiro:"
- 'textbox "Ex: Shadows of Neon"'
- text: "Tagline / Subtítulo:"
- 'textbox "Ex: Na cidade fria, memórias mentem."'
- text: "Visibilidade inicial:"
- combobox:
  - option "Privado" [selected]
  - option "Compartilhado"
  - option "Público"
- button "Criar Novo Roteiro"
- main: Nenhum projeto selecionado Crie ou selecione um projeto para editar o roteiro
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Navegação CineWeave', () => {
  4  | 
  5  |   test.beforeEach(async ({ page }) => {
  6  |     await page.goto('/');
  7  |     await page.evaluate(() => localStorage.setItem('e2e-test', 'true'));
  8  |     await page.reload();
  9  |     await page.waitForSelector('.header-bar', { timeout: 15000 });
  10 |   });
  11 | 
  12 |   test('carrega app e mostra 4 nav tabs', async ({ page }) => {
  13 |     const tabs = page.locator('.nav-item');
  14 |     await expect(tabs).toHaveCount(4, { timeout: 10000 });
  15 | 
  16 |     const labels = await tabs.allTextContents();
  17 |     expect(labels.join(' ')).toContain('Roteiro');
  18 |     expect(labels.join(' ')).toContain('Enciclopédia');
  19 |     expect(labels.join(' ')).toContain('Mapa Mental');
  20 |     expect(labels.join(' ')).toContain('Ideias');
  21 |   });
  22 | 
  23 |   test('navega entre as 4 abas', async ({ page }) => {
  24 |     // Check that nav items exist
  25 |     await expect(page.locator('.nav-item')).toHaveCount(4, { timeout: 10000 });
  26 | 
  27 |     // Click Roteiro
  28 |     await page.locator('.nav-item', { hasText: 'Roteiro' }).click();
  29 |     await page.waitForTimeout(2000);
  30 |     const screenplayVisible = await page.locator('.screenplay-layout-container').isVisible().catch(() => false);
  31 |     if (!screenplayVisible) {
  32 |       // Debug: take screenshot
  33 |       await page.screenshot({ path: 'test-results/debug-roteiro.png' });
  34 |       console.log('Page URL:', page.url());
  35 |       console.log('Page content:', await page.locator('#root').innerHTML().catch(() => 'n/a').then(h => h.substring(0, 500)));
  36 |     }
> 37 |     await expect(page.locator('.screenplay-layout-container')).toBeVisible({ timeout: 15000 });
     |                                                                ^ Error: expect(locator).toBeVisible() failed
  38 | 
  39 |     await page.locator('.nav-item', { hasText: 'Enciclopédia' }).click();
  40 |     await expect(page.locator('.encyclopedia-container')).toBeVisible({ timeout: 15000 });
  41 | 
  42 |     await page.locator('.nav-item', { hasText: 'Mapa Mental' }).click();
  43 |     await expect(page.locator('.mindmap-container')).toBeVisible({ timeout: 15000 });
  44 | 
  45 |     await page.locator('.nav-item', { hasText: 'Ideias' }).click();
  46 |     await expect(page.locator('[data-onboarding="brainstorm-tab"]')).toBeVisible({ timeout: 15000 });
  47 |   });
  48 | 
  49 | });
  50 | 
```